<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>qdata-module-system</artifactId>
        <groupId>tech.qiantong</groupId>
        <version>3.8.8</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>qdata-module-system-biz</artifactId>

    <description>
        system系统模块
    </description>

    <dependencies>
        <!-- 核心模块 config-->
        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-config</artifactId>
            <version>3.8.8</version>
        </dependency>

        <!-- 系統模块 websocket-->
        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-websocket</artifactId>
            <version>3.8.8</version>
        </dependency>

        <!-- 系统模块 qdata-security -->
        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-security</artifactId>
            <version>3.8.8</version>
        </dependency>

        <!-- 系统模块 mybatis -->
        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-mybatis</artifactId>
            <version>3.8.8</version>
        </dependency>

        <!-- 系统模块 redis -->
        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-redis</artifactId>
            <version>3.8.8</version>
        </dependency>
<!-- ======================================================================== -->
        <!-- system-api模块 -->
        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-module-system-api</artifactId>
            <version>3.8.8</version>
        </dependency>

        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-module-ds-api</artifactId>
            <version>3.8.8</version>
            <scope>compile</scope>
        </dependency>

        <!-- 定时任务-->
        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-quartz</artifactId>
        </dependency>

        <!-- 代码生成-->
        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-generator</artifactId>
        </dependency>

        <!-- 通用工具-->
        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-common</artifactId>
        </dependency>

        <!-- 文件上传-->
        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-file</artifactId>
            <version>3.8.8</version>
        </dependency>

        <!-- pay-->
<!--        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-pay</artifactId>
            <version>3.8.8</version>
        </dependency>-->

        <!-- auth-->
        <dependency>
            <groupId>tech.qiantong</groupId>
            <artifactId>qdata-auth</artifactId>
            <version>3.8.8</version>
        </dependency>

        <!-- swagger3-->
<!--        <dependency>
            <groupId>io.springfox</groupId>
            <artifactId>springfox-boot-starter</artifactId>
        </dependency>-->

        <!-- 防止进入swagger页面报类型转换错误，排除3.0.0中的引用，手动增加1.6.2版本 -->
<!--        <dependency>
            <groupId>io.swagger</groupId>
            <artifactId>swagger-models</artifactId>
            <version>1.6.2</version>
        </dependency>-->
    </dependencies>

</project>
