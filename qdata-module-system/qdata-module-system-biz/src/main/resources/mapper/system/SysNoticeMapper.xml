<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tech.qiantong.qdata.module.system.mapper.SysNoticeMapper">

    <resultMap type="SysNotice" id="SysNoticeResult">
        <result property="noticeId" column="notice_id"/>
        <result property="noticeTitle" column="notice_title"/>
        <result property="noticeType" column="notice_type"/>
        <result property="noticeContent" column="notice_content"/>
        <result property="noticeContentText" column="notice_content_text"/>
        <result property="status" column="status"/>
        <result property="topFlag" column="top_flag"/>
        <result property="alertFlag" column="alert_flag"/>
        <result property="alertStartTime" column="alert_start_time"/>
        <result property="alertEndTime" column="alert_end_time"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectNoticeVo">
        <choose>
            <when test="@tech.qiantong.qdata.mybatis.config.MasterDataSourceConfig@getDatabaseType() == 'mysql'">
                SELECT notice_id, notice_title, notice_type,
                notice_content_text,
                status, top_flag, alert_flag, alert_start_time, alert_end_time,
                create_by, create_time, update_by, update_time, remark
                FROM system_notice
            </when>
            <when test="@tech.qiantong.qdata.mybatis.config.MasterDataSourceConfig@getDatabaseType() == 'dm8'">
                SELECT notice_id, notice_title, notice_type,
                notice_content_text,
                status, top_flag, alert_flag, alert_start_time, alert_end_time,
                create_by, create_time, update_by, update_time, remark
                FROM system_notice
            </when>
            <when test="@tech.qiantong.qdata.mybatis.config.MasterDataSourceConfig@getDatabaseType() == 'kingbase8'">
                SELECT notice_id, notice_title, notice_type,
                notice_content_text,
                status, top_flag, alert_flag, alert_start_time, alert_end_time,
                create_by, create_time, update_by, update_time, remark
                FROM system_notice
            </when>
        </choose>
    </sql>

    <select id="selectNoticeById" parameterType="Long" resultMap="SysNoticeResult">
        <include refid="selectNoticeVo"/>
        where notice_id = #{noticeId}
    </select>

    <select id="selectNoticeList" parameterType="SysNotice" resultMap="SysNoticeResult">
        <include refid="selectNoticeVo"/>
        <where>
            <if test="noticeType != null and noticeType != ''">
                AND notice_type = #{noticeType}
            </if>
            <if test="alertFlag != null">
                AND alert_flag = #{alertFlag}
            </if>
            <if test="topFlag != null">
                AND top_flag = #{topFlag}
            </if>

            <!-- 生效时间在 alert_start_time 和 alert_end_time 之间 -->
            <if test="params.efftectTime != null and params.efftectTime != ''">
                AND (
                (
                alert_start_time IS NULL AND alert_end_time IS NULL
                )
                OR (
                alert_start_time IS NOT NULL AND alert_end_time IS NOT NULL
                AND #{params.efftectTime} <![CDATA[ >= ]]> alert_start_time
                AND #{params.efftectTime} <![CDATA[ <= ]]> alert_end_time
                )
                )
            </if>
            <choose>
                <!-- 如果数据库是 kingBase -->
                <when test="@tech.qiantong.qdata.mybatis.config.MasterDataSourceConfig@getDatabaseType() == 'kingbase8'">
                    <if test="createBy != null and createBy != ''">
                        AND create_by LIKE '%' || #{createBy} || '%'
                    </if>
                    <if test="noticeTitle != null and noticeTitle != ''">
                        AND notice_title LIKE '%' || #{noticeTitle} || '%'
                    </if>
                </when>

                <!-- 如果是其他数据库（如 MySQL、 DM） -->
                <otherwise>
                    <if test="createBy != null and createBy != ''">
                        AND create_by like concat('%', #{createBy}, '%')
                    </if>
                    <if test="noticeTitle != null and noticeTitle != ''">
                        AND notice_title like concat('%', #{noticeTitle}, '%')
                    </if>
                </otherwise>
            </choose>
        </where>
    </select>

    <insert id="insertNotice" parameterType="SysNotice">
        insert into system_notice (
        <if test="noticeTitle != null and noticeTitle != '' ">notice_title,</if>
        <if test="noticeType != null and noticeType != '' ">notice_type,</if>
        <if test="noticeContent != null and noticeContent != '' ">notice_content,</if>
        <if test="noticeContentText != null and noticeContentText != '' ">notice_content_text,</if>
        <if test="status != null and status != '' ">status,</if>
        <if test="topFlag != null">top_flag,</if>
        <if test="alertFlag != null">alert_flag,</if>
        <if test="alertStartTime != null">alert_start_time,</if>
        <if test="alertEndTime != null">alert_end_time,</if>
        <if test="remark != null and remark != ''">remark,</if>
        <if test="createBy != null and createBy != ''">create_by,</if>
        create_time
        ) values (
        <if test="noticeTitle != null and noticeTitle != ''">#{noticeTitle},</if>
        <if test="noticeType != null and noticeType != ''">#{noticeType},</if>
        <if test="noticeContent != null and noticeContent != ''">#{noticeContent},</if>
        <if test="noticeContentText != null and noticeContentText != ''">#{noticeContentText},</if>
        <if test="status != null and status != ''">#{status},</if>
        <if test="topFlag != null">#{topFlag},</if>
        <if test="alertFlag != null">#{alertFlag},</if>
        <if test="alertStartTime != null">#{alertStartTime},</if>
        <if test="alertEndTime != null">#{alertEndTime},</if>
        <if test="remark != null and remark != ''">#{remark},</if>
        <if test="createBy != null and createBy != ''">#{createBy},</if>
        <choose>
            <when test="@tech.qiantong.qdata.mybatis.config.MasterDataSourceConfig@getDatabaseType() == 'dm8'">
                sysdate
            </when>
            <otherwise>
                now()
            </otherwise>
        </choose>
        )
    </insert>

    <update id="updateNotice" parameterType="SysNotice">
        update system_notice
        <set>
            <if test="noticeTitle != null and noticeTitle != ''">notice_title = #{noticeTitle},</if>
            <if test="noticeType != null and noticeType != ''">notice_type = #{noticeType},</if>
            <if test="noticeContent != null">notice_content = #{noticeContent},</if>
            <if test="noticeContentText != null">notice_content_text = #{noticeContentText},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="topFlag != null">top_flag = #{topFlag},</if>
            <if test="alertFlag != null">alert_flag = #{alertFlag},</if>
            <if test="alertStartTime != null">alert_start_time = #{alertStartTime},</if>
            <if test="alertEndTime != null">alert_end_time = #{alertEndTime},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <choose>
                <when test="@tech.qiantong.qdata.mybatis.config.MasterDataSourceConfig@getDatabaseType() == 'dm8'">
                    update_time = sysdate
                </when>
                <otherwise>
                    update_time = now()
                </otherwise>
            </choose>
        </set>
        where notice_id = #{noticeId}
    </update>

    <delete id="deleteNoticeById" parameterType="Long">
        delete
        from system_notice
        where notice_id = #{noticeId}
    </delete>

    <delete id="deleteNoticeByIds" parameterType="Long">
        delete from system_notice where notice_id in
        <foreach item="noticeId" collection="array" open="(" separator="," close=")">
            #{noticeId}
        </foreach>
    </delete>

</mapper>
